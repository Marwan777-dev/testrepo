/**
 * A persona bound to a journey, as embedded in the journey detail tree. Populated in US-3;
 * empty array until persona binding lands.
 */
export interface PersonaBinding {
  personaId: string
  nameAr: string
  nameEn: string
}
